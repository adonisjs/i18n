'use strict'

const Schema = use('Schema')

class LocalesTableSchema extends Schema {

  up () {
    this.create('locales', table => {
      table.increments()
      table.string('locale', 5).notNullable()
      table.string('group', 100).notNullable()
      table.string('item', 255).notNullable()
      table.text('text', 'longtext').notNullable()
      table.timestamps()
    })
  }

  down () {
    this.drop('locales')
  }

}

module.exports = LocalesTableSchema
